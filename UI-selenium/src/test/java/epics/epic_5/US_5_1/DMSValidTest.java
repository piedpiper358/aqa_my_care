package epics.epic_5.US_5_1;

import io.qameta.allure.Owner;
import io.qameta.allure.Severity;
import io.qameta.allure.SeverityLevel;
import io.qameta.allure.TmsLink;
import org.junit.jupiter.api.*;
import pages.AuthorizationPage;
import pages.MainPage;

public class DMSValidTest {
/*    private MainPage mainPage;
    private AuthorizationPage authorizationPage;
    private CalculatorFromLifePage calculatorFromLifePage;
    private CalculatorFormKACKOPage calculatorFormKACKOPage;
    private KACKOPaymentPage kackoPaymentPage;
    @BeforeAll
    public void registerUserForAuthorization() {
        mainPage = new MainPage();
    }
    @AfterEach
    public void goToMainPage(){
        mainPage.openPage(BASE_URI);
    }
    @Owner(value = "Миргалеев Денис")
    @DisplayName("Проверка оформления полиса ДМС с валидными данными пользователя ")
    @Severity(value = SeverityLevel.NORMAL)
    @Test
    public void checkCreateApplicationWithValidData() throws InterruptedException {
        loginAndOpenPageCreateApplication();
        calculatorFromLifePage.clickButtonIssueAppLife()
                .clickButtonOlderAgeGroup()
                .clickFastSelectSumInsureThreeHundred()
                .clickRadioButtonPerMonth()
                .clickApplyButton();
        createInsurerPerson();
        calculatorFromLifePage.clickCheckBox(calculatorFromLifePage.isInsurantCheckBox)
                .clickCheckBox(calculatorFromLifePage.agreementCheckBox)
                .clickSubmit();
        mainPage.waitForElementToBeVisible(popup);
        Assertions.assertTrue(popup.isDisplayed());
    }*/
}